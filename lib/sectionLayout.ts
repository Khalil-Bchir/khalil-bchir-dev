/**
 * Home page section layout — keep in sync with AboutSection (source of truth).
 */
export const homeSectionPadding =
  "px-4 py-16 sm:px-6 sm:py-20 md:px-10 md:py-24";

export const homeSectionInner = "mx-auto w-full max-w-4xl";

/** Vertical rhythm between major blocks inside the inner container */
export const homeSectionStackGap = "space-y-6 sm:space-y-8";
